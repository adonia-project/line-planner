import * as React from 'react';
import './add-station.scss';

export interface AddStationProps { compiler: string; framework: string; }

export class AddStation extends React.Component<AddStationProps, {}> {
    render() {
        return (
            <div>

            </div>
        );
    }
}
